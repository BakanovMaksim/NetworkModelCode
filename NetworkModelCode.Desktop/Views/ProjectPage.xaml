<Page x:Class="NetworkModelCode.Desktop.Views.ProjectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NetworkModelCode.Desktop.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProjectPage">

    <Page.Resources>
        <Style x:Key="dataGridStyle" TargetType="DataGrid">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Page.Resources>
    
    <Grid x:Name="projectGrid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <TabControl x:Name="mathModelTabControl" Grid.Row="0" Grid.Column="1">
            <TabItem x:Name="tableTabItem" Header="Таблица">
                <DataGrid x:Name="tableDataGrid" ItemsSource="{Binding WorkTimeCharacteristicsDTO}" Style="{StaticResource dataGridStyle}" AutoGeneratingColumn="tableDataGrid_AutoGeneratingColumn">
                </DataGrid>
            </TabItem>
            <TabItem x:Name="graphTabItem" Header="Сетевой график"/>
        </TabControl>
        <Button x:Name="enterButton" Grid.Row="1" Grid.Column="1" Click="enterButton_Click" Margin="100"/>
    </Grid>
</Page>
